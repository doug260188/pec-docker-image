pipeline {
    agent any

    stages {
        stage('Run Container') {
            steps {
                script {
                    //Baixando dockerfile
                    sh 'wget https://github.com/doug260188/pec-docker-image/dockerfile'

                    //Construindo imagem
                    sh 'docker build -t doug260188/debina:latest .'
                }
                
            }
        }
    }
}

