pipeline {
    agent any
    stages {
        stage('Run Container') {
            steps {
                sh 'export PATH=$PATH:/usr/bin'
                sh 'docker-compose up -d /home/rodrigo/pec-docker-image/docker-compose.yml'
            }
        }
    }
}

